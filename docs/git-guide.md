# 🐙 Nixa Duty | Полный гайд по Git и GitHub для команды Frontend и Backend v1.0

## 1. Назначение документа

Этот документ служит **путеводителем по работе с Git и GitHub** для всей команды проекта Nixa Duty.  
Он охватывает:

- Установку Git на Windows  
- Основные команды терминала  
- Работа с ветками и слияниями  
- Использование графического интерфейса IDE JetBrains (PhpStorm/WebStorm)  
- Правила коммитов и workflow  
- Визуализацию веток для наглядного понимания  
- Рекомендации для начинающего разработчика  

Документ рассчитан даже на новичков. Примеры приведены для понимания, не для слепого повторения.

---

## 2. Установка Git на Windows

### 2.1 Скачивание и установка

1. Перейдите на официальный сайт Git: [https://git-scm.com/download/win](https://git-scm.com/download/win)  
2. Выберите версию для Windows.  
3. Следуйте мастеру установки:
   - Используйте **Git Bash** как терминал по умолчанию  
   - Разрешите добавление Git в PATH  
   - Рекомендуем включить опцию `Checkout Windows-style, commit Unix-style line endings`  

### 2.2 Проверка установки

Откройте Git Bash и выполните:

```bash
git --version
```

Вы должны увидеть версию Git, например:

```
git version 2.42.0.windows.1
```

---

## 3. Настройка Git для проекта

```bash
# Установите глобальное имя пользователя
git config --global user.name "Ваше Имя"

# Установите глобальный email
git config --global user.email "email@example.com"

# Проверка настроек
git config --list
```

Для GitHub используйте **тот же email**, что и в аккаунте, для корректного связывания коммитов.

---

## 4. Основные команды Git для новичков

| Команда                     | Описание                              |
| --------------------------- | ------------------------------------- |
| `git init`                  | Инициализация нового репозитория      |
| `git clone <url>`           | Клонирование репозитория с GitHub     |
| `git status`                | Состояние файлов, текущая ветка       |
| `git add <файл>`            | Добавление изменений в staging        |
| `git commit -m "сообщение"` | Сохранение изменений с комментарием   |
| `git push origin <ветка>`   | Отправка локальных коммитов на GitHub |
| `git pull`                  | Обновление локального репозитория     |
| `git branch`                | Список веток                          |
| `git checkout <ветка>`      | Переключение между ветками            |
| `git merge <ветка>`         | Слияние ветки в текущую               |

Эти команды нужны для работы через терминал. Графический интерфейс IDE также выполняет все эти действия.

---

## 5. Работа с GitHub через JetBrains IDE

### 5.1 Настройка

1. IDE: **PhpStorm** или **WebStorm**  
2. Скачивание: [https://www.jetbrains.com/](https://www.jetbrains.com/)  
   - В РФ сайт JetBrains доступен только через VPN  
3. Активация IDE: запросите код активации у администратора проекта

### 5.2 Полезные плагины для IDE

| Плагин           | Назначение                                    |
|-----------------|-----------------------------------------------|
| GitToolBox       | Расширенные функции Git, автообновление ветки |
| .ignore          | Автоматическое игнорирование файлов           |
| CodeGlance       | Мини-карта кода для быстрого поиска           |
| Rainbow Brackets | Подсветка скобок для удобства чтения          |

### 5.3 Использование GUI

1. Откройте проект в IDE  
2. Меню `VCS → Git → Clone` для клонирования  
3. Коммиты через `Commit` (Ctrl+K)  
4. Push через `VCS → Git → Push`  
5. Pull через `VCS → Git → Pull`  
6. Создание ветки через `VCS → Git → Branches → New Branch`  

---

## 6. Ветки и workflow (с визуализацией)

### 6.1 Стандартные ветки проекта

- **main** — стабильная, релизная ветка  
- **develop** — интеграционная ветка  
- **feature/<имя>** — ветки для новых фич  
- **hotfix/<имя>** — ветки для срочных исправлений  

### 6.2 Правила работы

1. Каждая фича — отдельная ветка  
2. Коммиты — логические и небольшие  
3. Перед пушем синхронизируйтесь с `develop` (`git pull develop`)  
4. Слияние веток только после проверки  

### 6.3 Визуализация веток

```text
                               main (релизы)
                                  │
                                  ● commit #1: v1.0 release
                                  ● commit #2: исправление багов
                                  │
                           develop ────────────── интеграция фич
                                  │
                                  ● commit #3: подготовка к новым фичам
                                  │
           ┌──────────────────────┼─────────────────────┐
           │                      │                     │
      feature/login          feature/ui           hotfix/fix-swap
           │                      │                     │
           ● commit #4            ● commit #6           ● commit #8
           ● commit #5            ● commit #7           └─ merge → develop → main
           └─ merge → develop      └─ merge → develop
```

#### Пояснение

- **main** — только рабочий код  
- **develop** — интеграция фич  
- **feature/** — новые фичи  
- **hotfix/** — срочные исправления  
- Слияния обозначены стрелками `merge →`  

### 6.4 Цветовая схема

- 🔹 Синий — `main`  
- 🟢 Зелёный — `develop`  
- 🟡 Жёлтый — `feature/*`  
- 🔴 Красный — `hotfix/*`  

### 6.5 Резюме работы с ветками

- Каждая новая фича — отдельная ветка  
- Merge только через `develop` → `main`  
- Коммиты маленькие и логичные  
- Визуализация учебная, для понимания процесса  

---

## 7. Правила работы с коммитами

1. Коммит должен содержать **описание того, что сделано**  
2. Использовать английский или русский, кратко  
3. Один коммит = одна логическая единица работы  

Примеры:

```text
commit #5: Добавлена валидация формы логина
commit #7: Стилизованы карточки студентов
commit #8: Исправлен баг обмена дежурств
```

---

## 8. Рекомендации для джуна

- Делайте коммиты локально и push только после проверки  
- Всегда синхронизируйтесь с `develop`  
- Если не уверены — уточняйте у тимлида  
- Используйте GUI IDE, пока не уверены в терминале  

---

## 9. Дополнительно

- Используйте `.gitignore` для исключения лишних файлов  
- Не пушьте секретные ключи или пароли  
- Перед началом работы выполняйте `git pull develop`, чтобы избежать конфликтов  

---

Этот файл является **центральным руководством по Git для Nixa Duty**, актуальным для всех участников команды. Следуйте ему и сохраняйте порядок в ветках и коммитах.
