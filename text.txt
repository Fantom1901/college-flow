--------------------------------------------------
Файл: ./docs/api_spec.md
--------------------------------------------------
# 🔌 Nixa Duty Core | TOTAL API SPECIFICATION v1.0
> **Architect:** Nixa_io (Тимофей)
> **Contact:** @Nixa_io (Telegram)
> **Security:** TG InitData Validation

---

## 1. ФУНДАМЕНТ И БЕЗОПАСНОСТЬ

### 1.1 Базовый URL
`***`

### 1.2 Авторизация (Telegram WebApp Auth)
Аркадий передает в каждом заголовке данные, полученные от Telegram. На бекэнде проходит валидация.
- **Header:** `Authorization: <window.Telegram.WebApp.initData>`
- **Валидация:** Бэкенд вычисляет HMAC-SHA256, сравнивает хеш и извлекает `user.id`.

### 1.3 Глобальные коды ответов
- `200 OK`: Успех.
- `201 Created`: Создана запись (инвайт, запрос на обмен).
- `401 Unauthorized`: Ошибка валидации данных Телеграма.
- `403 Forbidden`: Студент лезет в админку или пытается принять чужой обмен.
- `422 Unprocessable Entity`: Ошибка логики (например, обмен с самим собой).

---

## 2. СУЩНОСТИ И СВЯЗИ (DATA MODELS)

### 2.1 Иерархия связей:
1. **Group** (Группа) — корень системы.
2. **User** (Пользователь) — связан с одной Группой. Имеет `role` (superadmin, admin, moderator, user).
3. **Duty** (Дежурство) — привязано к Дате, Группе и Пользователю.
4. **Swap** (Обмен) — связывает двух Пользователей и две Даты дежурств.
5. **Attendance** (Посещаемость) — связывает Пользователя, Дату и Статус (present/absent).

---

## 3. ЭНДПОИНТЫ (ENDPOINTS)

### 3.1 Группы и Профиль
- `GET /me` — Получить инфо о себе, своей роли и своей группе.
- `GET /group/members` — Список группы (ID, имя, вес, кол-во дежурств).
- `POST /group/invite/generate` — (Admin/Moder) Создать токен приглашения.
- `PATCH /group/settings` — (Admin/Moder) Смена метода (weight/static).

### 3.2 Календарь и Дежурства
- `GET /duty/schedule?start_date=YYYY-MM-DD&end_date=YYYY-MM-DD` — Список дежурных на период.
- `GET /duty/today` — Оперативная инфа на текущий день.
- `POST /duty/mark-completed` — (Moder) Подтверждение, что дежурство выполнено (начисляет +1 к весу).

### 3.3 Система Обмена (Swap System) — ПОДРОБНО
1. **Создание:** `POST /swaps/request`
   - *Body:* `{"to_user_id": int, "my_date": "date", "target_date": "date"}`
2. **Список входящих:** `GET /swaps/incoming` — запросы, которые ждут ответа юзера.
3. **Список исходящих:** `GET /swaps/outgoing` — запросы, которые юзер отправил сам.
4. **Решение:** `POST /swaps/{id}/decision`
   - *Body:* `{"status": "accepted" | "declined"}`
   - *Logic:* При `accepted` бэкенд в одной транзакции меняет `user_id` в таблице `DutySchedule` для обеих дат.

### 3.4 Статистика и Честность
- `GET /stats/leaderboard` — Список всей группы, отсортированный по `duty_count` (кто больше всех работал) или по `weight` (кто первый на вылет).
- `GET /stats/attendance` — Процент посещаемости группы для графиков Дианы.

---

## 4. ПОДРОБНАЯ ЛОГИКА АЛГОРИТМОВ (BACKEND ONLY)

### 4.1 Метод "Вес" (Weight Algorithm)
При генерации нового дежурства:
1. Выбрать всех активных `User` группы.
2. Отсортировать по `weight` (ASC) и `last_duty_date` (ASC).
3. Взять первого.
4. После дежурства: `weight = weight + 1`.

### 4.2 Обработка пропусков
Если `Attendance.status == "absent"` в день дежурства:
- Вес **НЕ** увеличивается.
- Студент остается в топе очереди на ближайшую свободную дату.

---

## 5. ИНТЕГРАЦИЯ (FRONT-BACK)

### Формат обмена данными
Всегда JSON. Даты всегда `YYYY-MM-DD`.

**Пример ответа (Member Card):**
```json
{
  "user_id": 77,
  "name": "Аркадий",
  "stats": {
    "total_duties": 5,
    "weight": 2,
    "absences": 0
  },
  "is_on_duty_today": false
}


--------------------------------------------------
Файл: ./docs/backend-spec.md
--------------------------------------------------
# 🏛️ Nixa Duty Core | Backend Specification
> **Project Status:** Design Phase 🏗️
> **Author:** [Nixa_io](https://github.com/Ваш_Профиль)
> **Target:** College Attendance & Duty Automation System

---

## 🎯 Обзор проекта
Система автоматизации дежурств и учета посещаемости для колледжа. Реализуется через **Telegram Mini App**, объединяя удобство мессенджера и функциональность веб-приложения.

---

## 🛠 Технологический стек
| Слой         | Технология     | Назначение                       |
|:-------------|:---------------|:---------------------------------|
| **Language** | `Python 3.10+` | Основной язык логики             |
| **Bot API**  | `Aiogram 3.x`  | Взаимодействие с Telegram        |
| **Database** | `PostgreSQL`   | Надежное хранение данных         |
| **ORM**      | `SQLAlchemy`   | ООП-подход к работе с БД         |
| **Web API**  | `FastAPI`      | Связь бэкенда с React-фронтендом |

---

## 👥 Иерархия ролей (RBAC)
- [x] **SuperAdmin** — Полный доступ, создание групп.
- [x] **Admin (Куратор)** — Управление своей группой, назначение старосты.
- [x] **Moder (Староста)** — Настройка алгоритмов, подтверждение обменов.
- [x] **User (Студент)** — Просмотр данных, заявки на обмен.

---

## 📊 Алгоритмы распределения
### 1. Система весов (Dynamic Weight)
Приоритет отдается пользователю с минимальным коэффициентом `weight`.
- После дежурства: `weight += 1`.
- При пропуске: `weight` остается прежним (человек остается в топе очереди).

### 2. Статическое расписание (Static Loop)
Циклическая очередь на основе списка группы. Идеально для планирования на недели вперед.

---

## 💾 Схема базы данных (Entity Relationship)



### Основные модели:
1. **User**: Хранит профиль, роль и текущий «вес».
2. **Group**: Настройки метода ротации и привязка к куратору.
3. **DutySchedule**: Календарь назначенных дежурств.
4. **SwapRequest**: Логика обмена дежурствами между студентами.

---

## 🚀 Основной функционал API
### 📡 Endpoints для фронтенда (React)
- `GET /api/v1/group/{id}/status` — Текущие дежурные и статистика.
- `POST /api/v1/swaps/create` — Инициация обмена.
- `PATCH /api/v1/attendance/mark` — Отметка посещаемости.

### 🤖 Функции бота
- **Deep Linking**: Регистрация в группе по уникальной ссылке.
- **Push Notifications**: Уведомления о дежурстве за 24 часа.

---

## 📈 План разработки (Roadmap)
- [ ] Проектирование БД и моделей SQLAlchemy.
- [ ] Реализация ядра алгоритма "Weight System".
- [ ] Разработка API для связи с React.
- [ ] Интеграция с Aiogram (Mini App Launcher).
- [ ] Тестирование системы обмена.

---
*Generated by Nixa_io — Quality First.*


--------------------------------------------------
Файл: ./docs/db-schema-spec.md
--------------------------------------------------
# 🗄️ Nixa Duty Core | Database Schema Specification v1.0
> **Engine:** PostgreSQL (рекомендуется) / SQLite
> **ORM:** SQLAlchemy 2.0 (Declarative Base)
> **Naming Convention:** snake_case

---

## 1. ОБЩАЯ СТРУКТУРА (Entity-Relationship)
База строится на принципе иерархии: **Группа > Пользователи > События (Дежурства/Пропуски)**.

---

## 2. ТАБЛИЦЫ (TABLES)

### 2.1 Таблица `groups` (Учебные группы)
Хранит настройки распределения дежурств.
| Column | Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `id` | Integer | Primary Key, Autoincrement | Уникальный ID группы |
| `name` | String(50) | Not Null | Название (напр. "П-421") |
| `rotation_method` | Enum | 'weight', 'static' | Алгоритм дежурств |
| `curator_id` | Integer | Foreign Key (`users.id`) | Ссылка на куратора группы |
| `invite_token` | UUID | Unique, Index | Токен для входа по ссылке |
| `created_at` | DateTime | default=now() | Дата создания группы |

### 2.2 Таблица `users` (Пользователи)
Центральная таблица со всеми ролями и весами.
| Column | Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `id` | Integer | Primary Key | Внутренний ID |
| `tg_id` | BigInteger | Unique, Index, Not Null | Telegram ID пользователя |
| `username` | String(32) | Nullable | Ник в Telegram |
| `full_name` | String(128) | Not Null | ФИО для списков |
| `role` | Enum | 'admin', 'moder', 'user' | Роль в системе |
| `group_id` | Integer | Foreign Key (`groups.id`) | Группа студента |
| `weight` | Float | default=0.0 | Текущий вес для алгоритма |
| `is_active` | Boolean | default=True | Статус (отчислен/активен) |

### 2.3 Таблица `duty_schedule` (Календарь дежурств)
Расписание, которое видит пользователь.
| Column | Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `id` | Integer | Primary Key | |
| `date` | Date | Not Null, Index | Дата дежурства |
| `user_id` | Integer | Foreign Key (`users.id`) | Кто дежурит |
| `group_id` | Integer | Foreign Key (`groups.id`) | В какой группе |
| `status` | Enum | 'pending', 'done', 'skipped' | Статус выполнения |

### 2.4 Таблица `swap_requests` (Логика обмена)
Хранит историю и текущие запросы на смену дат.
| Column | Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `id` | Integer | Primary Key | |
| `sender_id` | Integer | Foreign Key (`users.id`) | Кто инициировал |
| `receiver_id` | Integer | Foreign Key (`users.id`) | Кому предложили |
| `sender_date` | Date | Not Null | Дата, которую отдает отправитель |
| `receiver_date` | Date | Not Null | Дата, которую хочет забрать |
| `status` | Enum | 'pending', 'accepted', 'declined' | Статус запроса |

### 2.5 Таблица `attendance_logs` (Посещаемость)
Статистика для графиков Дианы.
| Column | Type | Constraints | Description |
| :--- | :--- | :--- | :--- |
| `id` | Integer | Primary Key | |
| `user_id` | Integer | Foreign Key (`users.id`) | Студент |
| `date` | Date | Not Null | Дата проверки |
| `status` | Enum | 'present', 'absent', 'late' | Результат |

---

## 3. ИНДЕКСЫ И ПРОИЗВОДИТЕЛЬНОСТЬ
Для того чтобы API Аркадия летало, создаем:
1. `idx_user_tg_id`: Ускоряет вход пользователя в Mini App.
2. `idx_duty_date_group`: Ускоряет получение календаря для конкретной группы.
3. `idx_swap_receiver`: Ускоряет показ уведомлений об обмене для получателя.

---

## 4. ТРИГГЕРЫ И БИЗНЕС-ЛОГИКА (SQLAlchemy Hooks)

1. **On Swap Accept:** При смене статуса `swap_requests.status` на 'accepted' -> автоматически обновить `user_id` в соответствующих записях `duty_schedule`.
2. **On Duty Done:** При смене `duty_schedule.status` на 'done' -> автоматически выполнить `user.weight += 1.0`.
3. **On Absence:** При записи 'absent' в посещаемость -> проверить, было ли у юзера сегодня дежурство. Если да — перенести дежурство на следующую свободную дату БЕЗ увеличения веса.

---

## 5. ПРИМЕР СВЯЗИ (Python Code Snippet)
```python
class User(Base):
    __tablename__ = "users"
    group = relationship("Group", back_populates="members")
    duties = relationship("DutySchedule", back_populates="user")


--------------------------------------------------
Файл: ./docs/designer_spec.md
--------------------------------------------------
# 🎨 UI/UX Design Specification: Nixa Duty Mini App
> **Роль:** Дизайнер интерфейсов
> **Инструмент:** Figma
> **Куратор проекта:** Тимофей (Nixa)

---

## 🌟 Твоя задача
Диана, твоя цель — сделать приложение, которым будет ПРИЯТНО пользоваться. Оно должно выглядеть как часть Telegram.

## 📍 С чего начать?
1. **Изучи базу:** Посмотри, как выглядят другие Mini Apps в Telegram (например, Wallet или боты с едой).
2. **Используй готовое:** Не рисуй кнопки с нуля. Найди в сообществе Figma "Telegram UI Kit". Это база готовых элементов (кнопок, иконок, полей ввода).

## 📱 Список экранов для отрисовки
- **Главная (Dashboard):** Кто дежурит сегодня? (Большая карточка с фото). Кто следующий? (Список ниже).
- **Статистика:** График посещаемости и список группы, где видно, кто сколько раз дежурил.
- **Обмен:** Экран, где кнопками можно выбрать одногруппника и предложить ему поменяться днями.
- **Админка (для старост):** Кнопки управления группой и генерация ссылки-приглашения.

## 💡 Важные правила
- **Цвета:** В Telegram есть темная и светлая темы. Твой дизайн должен работать в обеих. Используй системные цвета Telegram (напр., цвет текста, цвет фона).
- **Простота:** Чем меньше лишних деталей, тем лучше.

---

## 🆘 Если возникли трудности:
1. **Спроси Тимофея:** Он объяснит логику приложения.
2. **ИИ (ChatGPT/Claude/Gemini):** Можешь спросить: "Как лучше расположить элементы управления в мобильном приложении для списка студентов?".
3. **Pinterest/Behance:** Поищи идеи по запросу "Mobile UI Dashboard".


--------------------------------------------------
Файл: ./docs/duty-lifecycle-spec.md
--------------------------------------------------
# 🔁 Nixa Duty | Duty Lifecycle Specification v1.0

## 1. Назначение документа

Данный документ описывает жизненный цикл дежурства (Duty) в системе Nixa Duty и является
обязательным к исполнению для backend- и frontend-реализаций.

Документ фиксирует:
- допустимые состояния дежурства
- переходы между состояниями
- побочные эффекты (side-effects)
- инварианты системы

Любое поведение, не описанное в данном документе, считается некорректным.

---

## 2. Определение сущности Duty

Duty — это запись в таблице duty_schedule, описывающая назначенное дежурство
конкретного пользователя на конкретную дату в рамках одной группы.

Duty существует строго в одном состоянии в каждый момент времени.

---

## 3. Список состояний (State Enumeration)

Допустимые значения поля duty_schedule.status:

- pending — дежурство назначено, ожидает выполнения
- done — дежурство выполнено и подтверждено
- skipped — дежурство пропущено по причине отсутствия
- reassigned — дежурство переназначено системой

---

## 4. State Machine (Переходы состояний)

Состояния и допустимые переходы:

    pending → done
    pending → skipped
    pending → reassigned

Любые иные переходы запрещены.

---

## 5. Описание состояний и эффектов

### 5.1 pending

Условия:
- дата дежурства равна или больше текущей даты
- пользователь активен (users.is_active = true)

Разрешенные действия:
- участие в обмене (swap)
- переназначение
- подтверждение выполнения

Побочные эффекты:
- отсутствуют

---

### 5.2 done

Триггер:
- подтверждение старостой через API POST /duty/mark-completed

Побочные эффекты:
- users.weight увеличивается на 1
- создается запись в attendance_logs со статусом present

Ограничения:
- изменение запрещено
- обмен запрещен
- удаление запрещено

---

### 5.3 skipped

Триггер:
- в attendance_logs зафиксирован статус absent
- дата дежурства совпадает с текущей датой

Побочные эффекты:
- вес пользователя не изменяется
- пользователь сохраняет приоритет в очереди

Ограничения:
- изменение запрещено
- обмен запрещен

---

### 5.4 reassigned

Назначение состояния:
- фиксация факта переназначения дежурства

Триггеры:
- принятый swap
- деактивация пользователя
- ручное вмешательство Moder или Admin

Побочные эффекты:
- user_id в duty_schedule изменяется
- для нового пользователя дежурство имеет статус pending

---

## 6. Связь с Attendance

Соответствие статусов:

- pending — attendance отсутствует, вес не меняется
- done — attendance = present, вес +1
- skipped — attendance = absent, вес +0

Attendance:
- всегда привязана к дате
- не может существовать без пользователя

---

## 7. Инварианты системы

- у одного пользователя не может быть более одного дежурства на одну дату
- в одной группе не может быть более одного дежурства на одну дату
- дежурства со статусами done и skipped являются финальными
- обмен возможен только для дежурств в статусе pending

Нарушение любого инварианта приводит к ошибке 422 Unprocessable Entity.

---

## 8. Backend Enforcement

- все проверки выполняются в service-layer
- операции выполняются внутри транзакции БД
- при любой ошибке выполняется rollback
- frontend не имеет права реализовывать бизнес-логику

---

## 9. Frontend Contract

Frontend обязан:
- отображать состояние дежурства строго по status
- не предлагать действия, запрещенные текущим состоянием
- синхронизироваться с backend после каждого мутационного запроса

---

## 10. Приоритет документа

Данный документ является единственным источником истины
по жизненному циклу дежурств и имеет приоритет над UX- и API-описаниями
в случае расхождений.



--------------------------------------------------
Файл: ./docs/frontend-spec.md
--------------------------------------------------
# ⚡ Frontend Technical Specification: Nixa Duty Core
> **Роль:** Frontend-разработчик
> **Стек:** React + Vite + Tailwind CSS
> **Куратор проекта:** Тимофей (Nixa)

---

## 🚀 Аркадий, добро пожаловать в разработку!
Это твой первый проект, и это круто. Мы выбрали современный стек, который сейчас используют во всем мире. Не пугайся новых слов, всё разбирается по шагам.

## 🛠 Твой "Инструментальный ящик"
1. **Vite:** Инструмент, который запустит твой проект за секунду.
2. **React:** Библиотека для создания интерфейса из "кусочков" (компонентов).
3. **Tailwind CSS:** Способ быстро красить элементы (кнопки, текст) прямо в коде.
4. **Telegram SDK:** Библиотека, которая свяжет твой код с функциями Телеграма.

## 🏃‍♂️ Первые шаги (Твоя дорожная карта)
1. **Установка:** Установи Node.js и создай проект через команду `npm create vite@latest`.
2. **Компоненты:** Начни с малого. Сделай одну карточку студента. Потом вторую. Потом собери их в список.
3. **Telegram:** Подключи скрипт Telegram Web App, чтобы приложение "увидело", какой пользователь его открыл.

## 📂 Как структурировать код
- Каждая кнопка или плашка — это отдельный файл в папке `src/components`.
- Тимофей подскажет, как правильно разложить файлы, чтобы в них не запутаться.

---

## 🆘 Что делать если что то не понятно или код не работает:
1. **Тимофей:**  Спрашивай всё: от "как запустить?" до "почему тут ошибка?".
2. **Google & Stack Overflow:** Копируй ошибку из терминала и вставляй в поиск. 99%, что кто-то уже решал эту проблему.
3. **ИИ (ChatGPT/Gemini):** Твой личный ментор 24/7. Пиши: "Я пишу на React, хочу сделать список студентов, помоги с кодом".
4. **YouTube:** Посмотри уроки "React для начинающих".


--------------------------------------------------
Файл: ./docs/group-membership-policy.md
--------------------------------------------------
# 👥 Nixa Duty | Group Membership Policy v1.0

## 1. Назначение документа

Данный документ описывает правила **управления составом группы** в системе Nixa Duty.
Он обязателен для backend и frontend и гарантирует корректное поведение всех алгоритмов распределения дежурств.

Документ охватывает:
- добавление новых студентов
- деактивацию пользователей
- смену ролей
- удаление групп

---

## 2. Добавление студента ➕

### 2.1 Начальные параметры

- weight = среднее значение weight существующих пользователей группы
- is_active = true
- роль = user (по умолчанию)

### 2.2 Поведение

- новый студент участвует в генерации дежурств начиная со следующего цикла
- не получает задним числом дежурства
- включается в статистику посещаемости и лидерборд

---

## 3. Деактивация студента 🚫

### 3.1 Триггер

- users.is_active = false

### 3.2 Эффекты

- исключается из алгоритмов генерации
- все будущие pending duty:
  - меняют статус на reassigned
  - назначаются другим активным пользователям
- прошлые done/ skipped duty остаются неизменными

---

## 4. Смена ролей 🔄

- Admin назначает Moder
- старый Moder сохраняет все свои пользовательские данные
- смена роли не изменяет историю посещаемости и дежурств
- роли пользователей:
  - SuperAdmin > Admin > Moder > User
- права всегда наследуются сверху вниз

---

## 5. Удаление группы ❌

- может выполнять только SuperAdmin
- действия:
  - soft-delete группы
  - архивирование всех связанных данных:
    - users
    - duty_schedule
    - swap_requests
    - attendance_logs
- frontend должен скрывать удаленные группы из интерфейса

---

## 6. Backend Enforcement

- все изменения состава группы выполняются внутри транзакции БД
- при ошибке откат (rollback)
- frontend не может самостоятельно менять состав группы или роли пользователей

---

## 7. Инварианты

- один студент может принадлежать только к одной группе
- deactivated user:
  - не участвует в генерации
  - может быть реактивирован
- удаление группы очищает только будущие pending duty, done и skipped сохраняются в архиве



--------------------------------------------------
Файл: ./docs/main-spec.md
--------------------------------------------------
# 🧭 Nixa Duty | Developer Navigation Table v1.0

## 1. Назначение документа

Эта таблица служит **главным навигатором для всей команды разработки**, показывая, какие спецификации должны изучить каждый участник.
Файл обеспечивает быстрый доступ к Markdown-файлам на GitHub и минимизирует вопросы «где что лежит».

---

## 2. Таблица навигации

| Роль            | Спецификация                       | Ссылка на GitHub                                                                                                             |
|-----------------|------------------------------------|------------------------------------------------------------------------------------------------------------------------------|
| **Backend** 🖥️ | Duty Lifecycle                     | [duty-lifecycle-spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/duty-lifecycle-spec.md)                   |
| **Backend** 🖥️ | Schedule Generation                | [schedule-generation-spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/schedule-generation-spec.md)         |
| **Backend** 🖥️ | Group Membership Policy            | [group-membership-policy.md](https://github.com/Fantom1901/college-flow/blob/main/docs/group-membership-policy.md)           |
| **Backend** 🖥️ | API Specification                  | [api_spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/api_spec.md)                                         |
| **Backend** 🖥️ | Backend Specification              | [backend-spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/backend-spec.md)                                 |
| **Backend** 🖥️ | Database Schema                    | [db-schema-spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/db-schema-spec.md)                             |
| **Frontend** ⚡  | Frontend State Contract            | [frontend-state-contract.md](https://github.com/Fantom1901/college-flow/blob/main/docs/frontend-state-contract.md)           |
| **Frontend** ⚡  | Telegram UI Integration            | [telegram-ui-integration-spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/telegram-ui-integration-spec.md) |
| **Frontend** ⚡  | User Experience (UX) Specification | [user-experience-spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/user-experience-spec.md)                 |
| **Frontend** ⚡  | Frontend Technical Specification   | [frontend-spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/frontend-spec.md)                               |
| **Frontend** ⚡  | API Specification                  | [api_spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/api_spec.md)                                         |
| **Designer** 🎨 | User Experience (UX) Specification | [user-experience-spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/user-experience-spec.md)                 |
| **Designer** 🎨 | UI/UX Design Specification         | [designer_spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/designer_spec.md)                               |
| **Designer** 🎨 | Frontend Technical Specification   | [frontend-spec.md](https://github.com/Fantom1901/college-flow/blob/main/docs/frontend-spec.md)                               |

---

## 3. Рекомендации по использованию

- Открывать файлы напрямую по ссылкам на GitHub
- Начинать изучение с UX и жизненного цикла Duty
- Backend и Frontend разработчики должны сверяться с API и DB Schema
- Дизайнер должен учитывать ограничения фронтенда и правила Telegram Mini App

> Этот файл является **центральной точкой навигации** для всей команды, все изменения в спецификациях должны отражаться здесь.



--------------------------------------------------
Файл: ./docs/schedule-generation-spec.md
--------------------------------------------------
# 🗓️ Nixa Duty | Schedule Generation Specification v1.0

## 1. Назначение документа

Данный документ описывает правила **генерации расписания дежурств** в системе Nixa Duty.

Он определяет:
- когда запускается генерация
- на какой горизонт строится расписание
- как выбирается следующий дежурный
- какие ограничения обязаны соблюдаться

Документ обязателен для backend-реализации и используется frontend как источник логики отображения 📌.

---

## 2. Горизонт планирования 📆

Используется модель **скользящего окна (Rolling Window)**.

- Горизонт: **14 календарных дней вперед**
- Система обязана гарантировать:
  - наличие дежурств в будущем
  - отсутствие «пустых» дней внутри окна

---

## 3. Триггеры генерации ⚙️

### 3.1 Автоматическая генерация (Cron)

- Запуск: **ежедневно в 02:00** (локальное время сервера)
- Условие запуска:
  - если количество будущих дежурств < 14 дней
- Поведение:
  - догенерировать недостающие даты
  - существующие записи не изменяются

---

### 3.2 Ручная генерация (Moder / Admin)

- API endpoint: POST /duty/regenerate
- Доступ:
  - Moder
  - Admin

Ограничения:
- дежурства со статусом done не трогаются
- дежурство на текущий день не изменяется
- skipped не пересчитываются

Используется для:
- исправления ошибок
- изменений состава группы
- форс-мажоров 🔧

---

## 4. Общие правила генерации 🧠

Применяются **всегда**, независимо от алгоритма:

- не более одного дежурства:
  - на группу в день
  - на пользователя в день
- учитываются только пользователи:
  - users.is_active = true
- пользователь с уже назначенным duty в окне:
  - пропускается

---

## 5. Алгоритм генерации: Dynamic Weight ⚖️

Пошаговая логика:

1. Получить всех активных пользователей группы
2. Исключить пользователей, у которых уже есть duty в окне
3. Отсортировать список по:
   - weight (по возрастанию)
   - last_duty_date (по возрастанию)
4. Выбрать первого пользователя
5. Создать запись в duty_schedule со статусом pending

Важно:
- weight изменяется только после done
- skipped не влияет на сортировку

---

## 6. Алгоритм генерации: Static List 🔁

Используется фиксированная очередь.

Характеристики:
- порядок хранится на уровне группы
- порядок не меняется автоматически
- после конца списка очередь зацикливается

Поведение:
- система берет следующего по порядку
- если пользователь неактивен — пропускается

---

## 7. Праздники и выходные 🎉

Если в будущем будет добавлена таблица non_working_days:

- генерация пропускает такие даты
- дежурства на них не создаются
- окно 14 дней считается по рабочим дням

---

## 8. Атомарность и ошибки 🧱

- генерация выполняется внутри одной транзакции БД
- при любой ошибке:
  - выполняется полный rollback
- частично созданные расписания запрещены

---

## 9. Backend Enforcement

- генерация выполняется исключительно на backend
- frontend не имеет права:
  - рассчитывать очередь
  - выбирать пользователя
  - предлагать собственную логику

---

## 10. Приоритет документа 📌

Данный документ является источником истины
по генерации расписаний и имеет приоритет над:
- UX-документами
- frontend-логикой
- частными реализациями алгоритмов



--------------------------------------------------
Файл: ./docs/telegram-ui-integration-spec.md
--------------------------------------------------
# 📲 Nixa Duty | Telegram UI Integration Specification v1.0

## 1. Назначение документа

Этот документ описывает **интеграцию фронтенда с Telegram Mini App SDK** и
правила работы UI внутри мессенджера.

Frontend обязан строго следовать этому документу, чтобы:
- интерфейс был нативным для Telegram
- использовались доступные возможности Mini App
- соблюдались ограничения платформы

---

## 2. Основные элементы Telegram SDK

### 2.1 MainButton

- Используется для ключевых действий:
  - подтверждение дежурства
  - отправка swap-запроса
- Атрибуты:
  - text: string (надпись)
  - color: string (Hex/TG Theme)
  - visible: boolean
- Поведение:
  - кнопка видна только если действие разрешено по роли и статусу
  - при клике вызывает callback frontend, который отправляет запрос на backend

---

### 2.2 Haptic Feedback

- Вибрация при:
  - успешном выполнении действия
  - отклонении swap-запроса
- Типы:
  - light — краткая вибрация
  - medium — стандартная
  - heavy — при критических ошибках

---

### 2.3 ThemeParams

- Frontend должен подхватывать:
  - color_scheme: light / dark
  - accent_color
- Все элементы интерфейса (кнопки, карточки, графики) адаптируются к теме
- Стандартные системные цвета Telegram рекомендуется использовать для:
  - текста
  - фона
  - выделений

---

## 3. Навигация и экранные компоненты 🖥️

### 3.1 Dashboard

- Карточка текущего дежурного
- Список следующих дежурных
- Push-уведомление о дежурстве на завтра

### 3.2 Swap Screen

- Список одногруппников
- Возможность выбрать студента и предложить обмен
- Интерактивные уведомления и подтверждения через MainButton

### 3.3 Statistics / Leaderboard

- Отображение посещаемости и количества дежурств
- Графики должны менять цвет в зависимости от темы Telegram

### 3.4 Admin Panel (Moder / Admin)

- Генерация инвайт-ссылки
- Переключение алгоритма (Weight / Static)
- Кнопки управления видимы только пользователям с правами

---

## 4. Инварианты и ограничения ⚠️

- Frontend не должен:
  - имитировать backend-логику
  - создавать новые записи без API
  - отображать чужую информацию без прав
- Все действия пользователя через UI должны проходить **через backend**
- События Telegram SDK используются только для UX (feedback, main button, theme)

---

## 5. Рекомендации по UX

- Минимизировать количество шагов для ключевых действий
- Использовать нативные элементы Telegram (кнопки, карточки)
- Поддерживать обе темы: light / dark
- Обеспечивать реакцию на действия пользователя через визуальные и тактильные сигналы



--------------------------------------------------
Файл: ./docs/user-experience-spec.md
--------------------------------------------------
# 🧭 Duty Manager | Full User Experience (UX) Specification
> **Продукт:** Система автоматизации дежурств и посещаемости
> **Главный архитектор:** Тимофей (Nixa)
> **Статус:** Утверждено 🚀

---

## 🎭 1. Система ролей и прав
В системе существует строгая иерархия. Права вышестоящей роли включают все права нижестоящих.

| Роль           | Кто это     | Ключевые возможности                                            |
|:---------------|:------------|:----------------------------------------------------------------|
| **SuperAdmin** | Разработчик | Управление всеми группами, создание новых Кураторов.            |
| **Admin**      | Куратор     | Удаление/добавление студентов, назначение Старосты.             |
| **Moder**      | Староста    | Выбор алгоритма (Вес/Списки), подтверждение обмена дежурствами. |
| **User**       | Студент     | Просмотр очереди, запрос на обмен, просмотр статистики.         |

---

## 🔄 2. Путь пользователя (User Flow)

### 🚪 Вход в систему (Onboarding)
1. Пользователь получает уникальную ссылку от Старосты/Куратора.
2. При переходе в бота нажимает кнопку "Запустить приложение".
3. Система автоматически подхватывает его данные из Telegram (Имя, ID).
4. Пользователь сразу попадает в свою учебную группу.

### 📅 Ежедневное использование (Студент)
- **Проверка:** Открыв Mini App, студент видит, кто дежурит сегодня.
- **Уведомление:** Если студенту назначено дежурство на завтра, бот присылает ему личное сообщение.
- **Статистика:** Студент может зайти во вкладку "Лидерборд" и увидеть, что он дежурил 5 раз, а его сосед — 2 раза (прозрачность системы).

### 🤝 Механика обмена (Swap Logic)
1. Студент А понимает, что не может дежурить в четверг.
2. В приложении он нажимает "Поменяться" и выбирает Студента Б из списка группы.
3. Студенту Б приходит уведомление в боте: *"Студент А хочет поменяться с вами дежурствами. Принять?"*.
4. Если Студент Б нажимает "Да", система автоматически пересчитывает график, и Староста получает уведомление об успешном обмене.

---

## ⚙️ 3. Логика дежурств (Rotation Logic)

Староста может переключать режимы в настройках группы:

### Вариант А: "Система Веса" (Справедливость)
- У каждого есть счетчик дежурств.
- Система всегда выбирает того, у кого счетчик меньше.
- Если человек заболел (Староста пометил пропуск), его вес не растет, и он остается первым в очереди на следующий раз.

### Вариант Б: "По списку" (Предсказуемость)
- Обычная циклическая очередь.
- Пользователь может пролистать календарь на месяц вперед и увидеть все свои даты.

---

## 📊 4. Учет посещаемости
- Каждый учебный день Куратор или Староста открывает вкладку "Посещаемость".
- Список группы представлен в виде чек-бокса.
- Одним нажатием отмечаются присутствующие.
- Данные мгновенно улетают в базу и отображаются в общем графике статистики.

---

## 🆘 Помощь и поддержка
- По всем вопросам работы приложения обращаться к **Тимофею**.
- Ошибки и баги можно гуглить или спрашивать у ИИ, но лучше сразу писать в чат разработки.


